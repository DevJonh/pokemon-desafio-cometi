import HashLoader from "react-spinners/HashLoader";
import * as Styles from "./styles";

const Loading = () => {
  return (
    <Styles.Container>
      <HashLoader />;
    </Styles.Container>
  );
};

export default Loading;
